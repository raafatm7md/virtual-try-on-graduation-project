import 'package:flutter/material.dart';

class MenProducts extends StatelessWidget {
  const MenProducts({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Men'));
  }
}
